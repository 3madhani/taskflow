enum ProjectStatus {
  active,
  onHold,
  completed,
}

enum ProjectPriority {
  low,
  medium,
  high,
}

enum TaskStatus {
  pending,
  inProgress,
  done,
}

enum TaskPriority {
  low,
  medium,
  high,
}

enum AppButtonVariant {
  primary,
  outlined,
}
