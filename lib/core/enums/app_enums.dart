enum ProjectStatus {
  active,
  onHold,
  completed,
}

enum TaskStatus {
  pending,
  inProgress,
  done,
}

enum TaskPriority {
  low,
  medium,
  high,
}

enum AppButtonVariant {
  primary,
  outlined,
}
